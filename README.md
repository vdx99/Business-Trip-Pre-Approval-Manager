#Business-Trip-Pre-Approval-Manager

Simple internal service designed to automate the business trip preapproval process where users submit Business Trip Requests (BTR) that can be automatically approved based on configured validations or routed to an Approver for review.
